# 写作与编辑规范

这份规范统一文章的编辑质量，不统一作者的语气。随笔可以主观，教程可以直接，但事实、结构和排版使用同一套标准。

## 文章类型

### 随笔与评论

- 从一个具体观察、经历或疑问进入，不用空泛的时代判断开场。
- 区分“我感受到的”“我的解释”和“可验证的事实”。
- 保留锋利判断，但避免反复使用“不是 A，而是 B”“真正的问题是”等固定句式。
- 结尾回到文章最初的问题，不强求金句或宏大升华。

### 技术与知识文章

- 按“问题—原理—示例—结论”组织。
- 命令、文件名、API 和代码标识符使用行内代码。
- 截图只保留能帮助完成操作或理解概念的部分。
- 会随版本变化的结论注明验证日期或链接官方文档。

## Frontmatter

```yaml
---
title: 清楚、准确的标题
date: '2026-08-16'
slug: kebab-case-slug
summary: 一句话说明文章的核心价值，避免重复标题。
tags:
  - thinking
draft: false
---
```

- 日期统一为带引号的 `YYYY-MM-DD`。
- 每篇使用 1–3 个标签，标签不表达语言。
- 标签优先从以下集合选择：`life`、`thinking`、`history`、`film`、`tutorial`、`AI`、`Agent Economy`、`frontend`、`backend`、`css`、`react`、`vue.js`、`typescript`、`node`、`internet`、`github`、`startup`、`business`、`Crypto`。
- `summary` 只负责列表展示和默认 SEO 描述；确有搜索需求时再补 `seoTitle`、`seoDescription` 和 `seoKeywords`。

## 结构

- 页面标题由模板生成 H1，正文不再写 H1。
- 一级章节使用 H2（`##`），章节内部使用 H3（`###`）。
- 随笔可以不用编号；需要强调论证次序时，统一使用“一、二、三”。
- 标题必须表达内容，不使用“前言”“其他”“八、”等空标题。
- 单段只承担一个意思；连续短句可以保留，但不要整篇都写成单句段落。

## 语气

- 优先使用第一人称说明观察位置，不冒充全知视角。
- 事实用事实语气，推断明确写成推断，争议观点注明“我的理解”。
- 一个章节最多保留一个需要读者停下来的核心句。
- 粗体用于关键词或结论，不用于整段制造节奏。
- 不为吸引点击夸大确定性；标题的力度不能超过正文证据。

## 中文排版

- 中文使用全角标点：`，。；：？！“”`。
- 中文与英文、数字之间留一个空格，但专有名词内部不拆分，例如 `Vue 3`、`HTTP/2`。
- 书籍、电影和文章作品名使用《》；概念或短语使用引号。
- 范围使用连接号，例如 `30–60 分钟`；代码中的符号保持原样。

## 事实与引用

- 个人经历和审美判断不需要引用。
- 数据、人物原话、历史事实和会变化的产品能力必须链接来源。
- 优先使用原始资料、官方文档、论文和当事人采访。
- 三条以内随文链接；资料较多时在文末增加 H2“参考资料”。
- 有争议的历史解释同时说明它是哪个学者、文本或流派的看法。

## 图片

- 图片存放在 `public/images/posts/<slug>/`，优先使用 WebP。
- 随笔通常使用 1 张封面；长文确有抽象概念需要解释时再增加 1–2 张。
- 教程图片服务于步骤和结果，不做纯装饰。
- 每张图片必须有描述信息的 alt，不能使用 `image1`、文件名或空文本。

## 发布前检查

- 标题、摘要和正文讨论的是同一个问题。
- 正文从 H2 开始，没有空标题或跳级。
- 删除重复论点、过度粗体和模板化转折。
- 可验证事实有来源，链接能打开。
- 图片为本地 WebP 或稳定来源，并有有效 alt。
- 标签无重复且不包含语言标签。
- 运行测试和生产构建。
